export default class Contact {
  constructor({ id, name, phone, email, address, avatar }) {
    this.id = id
    this.name = name
    this.phone = phone
    this.email = email
    this.address = address
    this.avatar = avatar
  }
}
